# TODO

## Done

- Databricks bundle scaffold and job resources are in place (`databricks.yml`, `resources/`).
- Bootstrap creates `fin_signals_dev` + `bronze/silver/gold/audit` (`src/bootstrap/bootstrap_env.py`, `sql/001_create_catalog.sql`, `sql/002_create_schemas.sql`).
- Bronze ingestion path is operational with explicit schema and append-only Delta write (`src/bronze/ingest_market_data.py`, `sql/010_create_bronze_tables.sql`).
- Silver and Gold phases are integrated into the daily pipeline flow (`resources/jobs/daily_pipeline_job.yml`, `notebooks/02_silver_transform_demo.py`, `notebooks/03_gold_analytics_demo.py`).
- Audit logging writes to `fin_signals_dev.audit.pipeline_runs` (`src/common/audit.py`, `sql/003_create_audit_tables.sql`).

## Next

- Align repository Silver/Gold source and SQL artifacts with delivered pipeline behavior (close placeholder gaps where present).
- Expand validation checks in `notebooks/99_validation_queries.py` and `src/tests/`.
- Add clear data-contract notes for Silver and Gold tables in `docs/data_model.md`.
- Add operational checks (freshness, expected row counts, failure reasons) to audit/monitoring outputs.

## Later

- Consolidate duplicated logic between notebooks and `src/` modules into stable entrypoints.
- Parameterize environment-specific settings (cluster IDs, schedule, source symbols) via bundle variables.
- Extend runbook with failure playbooks per Bronze/Silver/Gold stage.
